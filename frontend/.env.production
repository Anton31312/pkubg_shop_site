# Production environment variables for React build
# Отключаем inline runtime chunk для совместимости с строгим CSP
INLINE_RUNTIME_CHUNK=false

# Генерируем source maps для отладки (опционально)
GENERATE_SOURCEMAP=false
